language: node_js
node_js:
  - "0.10"
before_script:
  - npm install grunt-cli -g
  - npm install bower -g
  - npm install
  - bower install
  - chmod -x deploy.sh
script:
  - grunt build
deploy:
  provider: script
  script: deploy.sh
